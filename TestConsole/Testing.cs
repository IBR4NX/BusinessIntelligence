string text = "Hello World";
string name = "Ibrahim";

name.Length          // الطول
name[0]              // حرف
name.ToUpper()       // أحرف كبيرة
name.ToLower()       // أحرف صغيرة
name.Contains("I")   // البحث
name.Trim()          // حذف المسافات
text.TrimStart();            // حذف المسافات من البداية
text.TrimEnd();
name.Replace(...)    // استبدال
name.Split(...)      // تقسيم
name.Substring(...)  // أخذ جزء
name.IndexOf(...)    // معرفة الموقع

Length         → طول النص
ToUpper()      → أحرف كبيرة
ToLower()      → أحرف صغيرة
Contains()     → يحتوي؟
StartsWith()   → يبدأ؟
EndsWith()     → ينتهي؟
IndexOf()      → موقع أول ظهور
LastIndexOf()  → موقع آخر ظهور
Replace()      → استبدال
Substring()    → أخذ جزء
Remove()       → حذف جزء
Trim()         → حذف مسافات الطرفين
TrimStart()    → حذف مسافات البداية
TrimEnd()      → حذف مسافات النهاية
Split()        → تقسيم النص
Join()         → دمج Array
Concat()       → دمج نصوص
Equals()       → مقارنة
Compare()      → مقارنة وترجع رقم
IsNullOrEmpty  → هل فارغ؟
new string()   → تكرار حرف
```

